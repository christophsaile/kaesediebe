---
import Navbar from '@components/Navbar.astro';
export interface IBaseHeader {
	arrowBack: boolean;
	share: boolean;
}

const { arrowBack, share } = Astro.props as IBaseHeader;
---

<header>
	<Navbar arrowBack={arrowBack} share={share} />
</header>
