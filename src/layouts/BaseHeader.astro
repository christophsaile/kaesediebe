---
import Navbar from '@components/Navbar.astro';
export interface IBaseHeader {
	arrowBack: boolean;
}

const { arrowBack } = Astro.props as IBaseHeader;
---

<header>
	<Navbar arrowBack={arrowBack} />
</header>
