import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.netlify.kaese-diebe',
  appName: 'kaesediebe',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
